.DS_Store
Thumbs.db
node_modules/
package-lock.json

# Componentes Jeff-Aporta: viven como submódulos en el monorepo Apps-fullstack,
# no aquí. Si reaparecen por accidente, no se versionan.
components/

# tests/ SE VERSIONA (4-ago-2026): los invariantes de deploy y de alineación con el ISS
# valen para todo el equipo, no solo para un checkout local.

# Carpetas ocultas (prefijo .) — excepto workflows CI
**/.*/
!/.github/
!/.github/**

# Respaldo del .git de submódulo roto (reparación 4-ago-2026): es local, nunca se versiona.
.git-roto.bak

scripts/_agent_commit_*.mjs

